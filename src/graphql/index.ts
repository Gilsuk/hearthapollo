import schema from "./schema";
import { Context, context } from "./context";
import { RootValue, rootValue } from "./root-value";

export default schema;
export { Context, context, RootValue, rootValue };
